# CLAUDE.md

警察庁交通事故統計データを R2 上の GeoParquet から集計・可視化するためのリポジトリ。

**このファイルは分析の前提知識そのものである。** 集計を始める前に必ず読むこと。
ここに書かれた定義を知らずに SQL を書くと、エラーにならずに間違った数字が出る。

---

## データの場所と接続

```python
import duckdb
URL = 'https://shi-works.com/geoparquet/npa-traffic-accident-analytics/honhyo_2019-2024.parquet'
con = duckdb.connect()
con.execute("INSTALL httpfs; LOAD httpfs;")
con.execute(f"CREATE VIEW honhyo AS SELECT * FROM read_parquet('{URL}')")
```

| | |
|---|---|
| 行数 | 1,895,275（2019〜2024年の本票、1行＝1事故） |
| サイズ | 96.6 MB（zstd-9、row group 131,072行 × 15） |
| 形式 | GeoParquet 1.0.0（`geometry` 列あり、EPSG:4326） |
| 実測 | ビュー作成 1.33秒 / 全6年の集計 2.05秒（転送は約4MB） |

**ファイル全体をダウンロードしないこと。** Parquet の列指向構造により、
`SELECT` した列の Range リクエストだけが飛ぶ。96.6MB のうち集計に使う列は
合計4.6MB程度しかない。座標系の列（`geometry`・緯度経度）が全体の73%を占めるが、
集計では読まれないので無視してよい。

DuckDB 拡張は `httpfs` / `spatial` / `h3`（community）がローカルで動作確認済み。

---

## 【最重要】文字コードの罠

コード表のラベルは**全角ハイフンマイナス U+FF0D** と**全角チルダ U+FF5E** を使う。
見た目がほぼ同じ別文字を1つ混ぜるとエラーにならず **0件が返る**。

| 表記 | 結果 |
|---|---:|
| `単路－3.5m未満`（U+FF0D）+ `0～24歳`（U+FF5E） | 13,027件 |
| `単路-3.5m未満`（ASCIIハイフン） | **0件** |
| `単路ー3.5m未満`（長音符 U+30FC） | **0件** |
| `単路−3.5m未満`（数学記号 U+2212） | **0件** |
| `0〜24歳`（波ダッシュ U+301C） | **0件** |
| `0~24歳`（ASCIIチルダ） | **0件** |

**リテラル文字列は必ずこのファイルからコピーすること。** 手で打たない。
0件が返ったら、まず文字コードを疑う。

---

## 列の一覧（主要なもの）

73列 + geometry。全列が文字列型（`資料年次` のみ int16）。

| 列 | 備考 |
|---|---|
| `資料年次` | int16。ファイル年次＝計上年。**年次集計の既定軸** |
| `発生日時_年` `_月` `_日` `_時` `_分` | 発生時点。年の意味が資料年次と異なる（後述） |
| `都道府県名` `警察署等名` `市区町村コード` | |
| `事故内容` | `負傷事故` / `死亡事故` の2値 |
| `死者数` `負傷者数` | 文字列。合計時は `CAST(... AS INTEGER)` |
| `車道幅員` | 13種。生活道路の判定に使う |
| `路線名` | 996種。`一般市町村道` `主要地方道－都道府県道` `一般都道府県道` など |
| `ゾーン規制` | `ゾーン30` / `規制なし` |
| `年齢（当事者A）` `（当事者B）` | **年齢層**であって実年齢ではない |
| `当事者種別（当事者A）` `（当事者B）` | |
| `人身損傷程度（当事者A）` `（当事者B）` | `死亡` / `負傷` / `損傷なし` / `対象外当事者` |
| `事故類型` | `人対車両` / `車両相互` / `車両単独` / `列車` |
| `道路形状` `歩車道区分` `昼夜` `天候` `路面状態` | |
| `地点_緯度（北緯）_10進数` `地点_経度（東経）_10進数` | 地図描画用 |

---

## 生活道路の定義

警察庁の統計上の生活道路は「車道幅員5.5m未満の道路」。
`車道幅員` は単路と交差点で意味が違う（交差点は**交差する2道路の幅員の組み合わせ**）。

`車道幅員` の全13値と全期間件数:

| 値 | 件数 | 5.5m未満か |
|---|---:|---|
| `単路－3.5m未満` | 49,739 | **該当** |
| `単路－3.5m以上`（3.5〜5.5m） | 121,102 | **該当** |
| `交差点－小（5.5m未満）－小` | 141,194 | **該当**（両道路とも5.5m未満） |
| `交差点－中（5.5m以上）－小` | 201,393 | **片側のみ**5.5m未満 |
| `交差点－大（13.0ｍ以上）－小` | 45,163 | **片側のみ**5.5m未満 |
| `単路－5.5m以上` | 444,945 | 非該当 |
| `単路－9.0m以上` | 191,794 | 非該当 |
| `単路－13.0m以上` | 146,202 | 非該当 |
| `単路－19.5m以上` | 39,249 | 非該当 |
| `交差点－中（5.5m以上）－中` | 286,189 | 非該当 |
| `交差点－大（13.0ｍ以上）－中` | 84,909 | 非該当 |
| `交差点－大（13.0ｍ以上）－大` | 51,411 | 非該当 |
| `一般交通の場所` | 91,985 | 幅員不明 |

**既定は厳密定義**（`単路－3.5m未満` / `単路－3.5m以上` / `交差点－小（5.5m未満）－小`）。

```sql
"車道幅員" IN ('単路－3.5m未満','単路－3.5m以上','交差点－小（5.5m未満）－小')
```

**この選択は結果を約2倍動かす。** 広義（片側のみ5.5m未満の `交差点－中（5.5m以上）－小`
を含める）にすると件数がほぼ倍増する。生活道路から幹線に出る出会い頭事故はそこに入るため
どちらも正当だが、**どちらを使ったかを必ず明示すること**。黙って選んではいけない。

注: `交差点－大（13.0ｍ以上）－中` などに含まれる `ｍ` は**全角小文字m（U+FF4D）**、
`5.5m` の `m` は半角。表内で混在している。コピーして使うこと。

---

## 年齢層の制約

`年齢（当事者A/B）` は**実年齢ではなく年齢層コード**。8値しかない。

`0～24歳` / `25～34歳` / `35～44歳` / `45～54歳` / `55～64歳` / `65～74歳` / `75歳以上` / `不明`

したがって**「若年層」として取り出せるのは `0～24歳` の1区分のみ**。
未就学児・小学生・高校生・大学生・若手社会人がすべて同じ箱に入る。

**「10代の事故」「未成年の事故」「20代の事故」は原理的に集計できない。**
そう聞かれたら、それらしい数字を出さずに、この制約を伝えること。

`不明` は当事者A側では114件（全期間）とほぼ無いが、B側では多い
（車両単独事故の相手方など）。B側を分母にする集計では効いてくる。

---

## 当事者A / B

- **当事者A = 第1当事者 = 過失が最も重い側**
- **当事者B = その相手方**

**被害者かどうかで決まる区分ではない。** ただし実態としては大半が被害側（2024年）:

| | 損傷なし | 負傷 | 死亡 | 対象外 |
|---|---:|---:|---:|---:|
| 当事者A | 228,943 | 61,215 | 714 | 23 |
| 当事者B | 48,874 | 223,617 | 1,687 | 16,717 |

Bが死傷しているのは77.5%。一方で**Bが無傷の事故も16.8%、Aが死傷した事故も21.3%**ある。
人対車両では歩行者がB側なのが96.5%だが、A側の歩行者も1,351件あり、
そのほぼ全員が負傷している（＝負傷したが過失が重いと判断された歩行者）。

### 年齢と当事者種別は同一当事者でペア判定する

「年齢A」と「種別B」を掛け合わせると**別人の属性を混ぜることになる**。

```sql
-- 正しい: 若年の歩行者・自転車が関与した事故
count(*) FILTER (
     ("年齢（当事者A）"='0～24歳' AND "当事者種別（当事者A）" IN ('歩行者','軽車両－自転車','軽車両－駆動補助機付自転車'))
  OR ("年齢（当事者B）"='0～24歳' AND "当事者種別（当事者B）" IN ('歩行者','軽車両－自転車','軽車両－駆動補助機付自転車'))
)
```

`count(*) FILTER (...)` で**行単位に数える**こと。A側・B側を別々に数えて足すと、
双方が条件に合致する事故（若年自転車同士など、2019年で58件）を二重計上する。

### 自転車の範囲

`軽車両－自転車` と `軽車両－駆動補助機付自転車`（電動アシスト）を含める。
`特定小型原付自転車`（電動キックボード等、2024年〜）は**別区分**として扱い、含めない。

### A/B判定の不連続（時系列比較の注意）

生活道路上で自転車が第1当事者と判定される割合が**2022年に不連続に変化している**。

| 年 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---:|---:|---:|---:|---:|---:|
| 自転車A側率 | 18.3% | 20.6% | 21.8% | **26.6%** | 27.2% | 28.2% |

2021→2022 に段差がある。事故実態の変化か過失認定の運用変更かは**このデータからは判別できない**。
A/B別の時系列比較をするときは必ず注記すること。

---

## 年次基準

「年」の意味が2つある。**既定は `資料年次`。**

年次ファイルには前年末に発生して当年に計上された事故が毎年8,000〜9,000件含まれる。

| 発生年＼ファイル | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 発生年 合計 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 2018以前 | 10,394 | 53 | 11 | 3 | 1 | 0 | 10,462 |
| 2019 | 370,843 | 9,321 | 28 | 5 | 2 | 1 | 380,200 |
| 2020 | | 299,804 | 8,069 | 28 | 5 | 1 | 307,907 |
| 2021 | | | 297,088 | 8,380 | 32 | 3 | 305,503 |
| 2022 | | | | 292,423 | 9,071 | 37 | 301,531 |
| 2023 | | | | | 298,819 | 8,477 | 307,296 |
| 2024 | | | | | | 282,376 | **282,376** |
| **ファイル合計** | **381,237** | **309,178** | **305,196** | **300,839** | **307,930** | **290,895** | |

| | `資料年次`（計上年） | `発生日時_年`（発生年） |
|---|---|---|
| 公表値との一致 | **一致する**（上の太字が警察庁公表の発生件数そのもの） | 一致しない |
| 最新年の完全性 | 確定済み | **約3%過少**。翌年ファイルで追加される |
| 事故の発生時点 | 最大1年ずれる | 正確 |
| 月次・季節性 | 不可 | 可能 |

**経年推移で `発生日時_年` を使うと最新年だけが見かけ上3%低く出る。**
実際の減少と区別がつかなくなるので、trend を語るときは `資料年次` を使うこと。
月次推移・季節性を見る場合のみ `発生日時_年` + `発生日時_月` を使う。

---

## 結果の見せ方

### 表

ターミナルに Markdown 表で出す。件数だけでなく**指数（基準年=100）と構成比を併記**する。
2020年はコロナ禍で全体件数が前年比19%減しており、実数だけでは傾向を読み誤る。

### 地図・グラフ（Artifact）

Artifact として公開したページは**外部へのfetch/XHRがCSPで全てブロックされる**。
R2 からデータを取得できないし、地図タイルも読めない。
**結果を埋め込んだ自己完結ページ**にすること。

| 用途 | 埋め込みサイズ | 可否 |
|---|---:|---|
| 集計表・経年推移グラフ | 数KB | ○ |
| 都道府県別コロプレス（境界込み） | 307 KB | ○ |
| 絞り込んだ点データ（例: 8,300件） | 約150 KB | ○ |
| 全1,895,275件の点表示 | 数十MB | **×**（既存 viewer + PMTiles の担当） |

都道府県境界: `../archive/npa-accident-hotspot-analyzer/input/prefecture.parquet`（47件、EPSG:6668）。
元は離島込みで121,990ポリゴン・21.6MB。**小ポリゴンを落として単純化**すると
115ポリゴン・307KB になる（`simplify` だけでは離島が残り縮まない）。

```python
def prune(geom, min_area=0.002):
    parts = list(geom.geoms) if geom.geom_type == 'MultiPolygon' else [geom]
    keep = [p for p in parts if p.area >= min_area] or [max(parts, key=lambda p: p.area)]
    return MultiPolygon(keep) if len(keep) > 1 else keep[0]
# → .simplify(0.005) → 座標を小数3桁に丸める
```

背景地図タイルは読めないが、集計地図なら境界ポリゴン自体が背景になるので実用上問題ない。

---

## 既知の制約

1. **本票は人身事故のみ。** 物損事故は含まれない
2. **2019〜2021年は12列が空値**（日の出入時刻・オートマチック車・サポカー・
   認知機能検査経過日数・運転練習の方法）。元データに存在しない
3. **2019〜2021年の警察署等・高速路線コード**は2019年版のみ。
   2020・2021年に新設された署・路線は未収録で、該当レコードは空欄
4. **2024年のトンネル番号**は2023年版CSVを使用。10件程度が不正確な可能性
5. **死亡事故は全期間で16,266件**と少ない。細かく絞ると年あたり数件になり、
   トレンドとして読めなくなる。母数が小さいときは明示すること

---

## データ更新（新年次追加時）

1. 前段リポジトリ `../npa-traffic-accident-converter` で
   `python -m converter --all --merge` → `./export_geo.sh`
2. `pipeline/build_parquet.py` で `資料年次` 列の付与と row group / 圧縮の最適化
3. `aws s3 cp ... --profile r2-shiworks` で R2 へ。
   キーは `geoparquet/npa-traffic-accident-analytics/honhyo_2019-YYYY.parquet`
   （ファイル名に年次範囲が入るので immutable キャッシュのまま新キーになる）
4. このファイルの件数・URL を更新

詳細は [docs/OPERATIONS.md](docs/OPERATIONS.md)。

---

## 分析の作法

- **定義の選択を黙って行わない。** 生活道路の厳密/広義、年次基準、A/B の扱いは
  結果を大きく動かす。どれを使ったか必ず書く
- **母数の変化を無視しない。** 実数の増減は全体トレンドとの比較で初めて意味を持つ
- **因果を主張しない。** 「ゾーン30の効果で減った」はこのデータからは言えない。
  相関を示し、判別できないことは判別できないと書く
- **使った SQL を示す。** 結果とあわせて提示すれば、その場で検算できる

集計結果やSQLのファイル保存は行わない。R2 への1クエリが数秒で返るため、
保存するより都度実行するほうが速く、古い結果が残る事故も起きない。
